<div class="page-header">
  <h3>My Events</h3>
</div>


<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <div class="panel panel-default">

      <% @myattendedevents.each do |reservation| %>

      <div class="panel-heading">
        <h3 class="panel-title">
          <a href="/users/<%= reservation.event.user_id %>"> <%= reservation.user %></a>
          <small>
            Created <%= time_ago_in_words(reservation.event.created_at) %> ago
          </small>
        </h3>
      </div>

      <div class="panel-body">
        <a href="/events/<%= reservation.event.id %>">
          <img src="<%= reservation.event.image %>" class="img-responsive">
        </a>
        <p>
        </p>
        <ul class="media-list">
          <li class="media">
            <div class="media-body">
              <h6 class="media-heading">
                <a href="/users/<%= reservation.event.user_id %>"><%= reservation.event.user.username %></a>
                <small>
                  Over <%= time_ago_in_words(reservation.event.created_at) %> ago
                </small>
              </h6>
              <p> <%= reservation.event.caption %></p>
            </div>
          </li>
        </ul>
      </div>
      <% end %>

    </div>

  </div>
</div>
